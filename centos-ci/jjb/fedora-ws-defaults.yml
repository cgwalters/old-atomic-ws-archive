- defaults:
    name: fedora-ws-defaults
    node: atomic-sig-ci-slave01 
    quiet-period: 0
    wrappers:
      - ansicolor
      - workspace-cleanup
      - timestamps

- scm:
    name: fedora-ws-scms
    scm:
      - git:
          url: "https://github.com/cgwalters/centos-ci-skeleton"
          basedir: cciskel
      - git:
          url: "https://pagure.io/forks/walters/workstation-ostree-config.git"
          basedir: workstation-ostree-config
          branches:
            - f24-continuous-installer

      
